# Everything scripts/evaluate.py, scripts/explore.py, scripts/make_figures.py
# and scripts/check_docs.py need. Nothing else.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7

# The scripts in failed_attempts/ additionally need transformers, torch,
# unsloth and xgboost. They are left out on purpose. Nothing in results/
# depends on them.
